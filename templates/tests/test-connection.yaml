apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "deprecation.fullname" . }}-test-connection"
  labels:
    {{- include "deprecation.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "deprecation.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
