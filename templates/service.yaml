apiVersion: v1
kind: Service
metadata:
  name: {{ include "deprecation.fullname" . }}
  labels:
    {{- include "deprecation.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "deprecation.selectorLabels" . | nindent 4 }}
